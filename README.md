# Welcome to zubaleGeo app 👋

This is an [Expo](https://expo.dev) project created with [`create-expo-app`](https://www.npmjs.com/package/create-expo-app).

## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```

To run the app is required to create a dev build locally

- [Create dev build](https://docs.expo.dev/develop/development-builds/create-a-build/)
